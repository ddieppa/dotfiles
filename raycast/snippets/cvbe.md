I will provide a block of raw experience text. Your job is to transform it into a **resume-ready, copy-and-paste section** using the formatting rules below. Do NOT add extra content beyond what is provided unless it improves clarity or readability.

### **Formatting Rules**

* Base structure

**Sr. Software Engineer (Consultant) — Company Name**
One-sentence summary describing the impact of the work at a high level (include the project name from the raw input, if provided).

    • Bullet point #1 (concise, senior-level, backend + cloud-focused)
    • Bullet point #2 (concise, senior-level, backend + cloud-focused)

* Add one blank line between the summary and the bullet points.
* Indent each bullet line with 4 spaces before the `•` marker so they appear nested under the summary.
* Always set the title to **Sr. Software Engineer** unless I explicitly request a different role.
* Use **bold** to highlight important technologies, frameworks, tools, and systems.
* Keep the summary and bullets **tight, senior-level, modern, technical**, never fluffy.
* Prioritize relevance for **Backend Engineer + Cloud Engineer (Azure)** unless I specify otherwise.
* Make each bullet **impactful**, emphasizing modernization, scalability, cloud architecture, distributed systems, performance improvements, integration work, and high-value contributions.
* When condensing, remove unnecessary details; keep only the strongest achievements, outcomes, and tech signals.

### **Tone & Style Requirements**

* Ultra-concise but high-density (strong hiring-manager energy).
* Modern tech vocabulary (Azure, .NET Core/6/8, microservices, event-driven, CI/CD, containers, distributed systems, performance optimization).
* Avoid repeating phrases across bullets.
* Maintain clarity, flow, and role alignment.

### **Output Requirements**

* Return exactly **one block** (Word-ready plain text) with no extra commentary.
* Show the exact output directly—**no code blocks, no extra markup**, exactly as it should appear in the resume.
* Keep `**bold**` markers for the title/company and key tech/impact phrases.
* Indent each bullet line with 4 spaces before the `•` marker.
* Preserve spacing so it pastes cleanly into Word or web editors.
* Do NOT use ALL CAPS for emphasis; rely on bold markers.

### **How to Use**

* Keep this prompt intact, then add a final line that begins with `RAW INPUT:` followed immediately by the raw experience text.
* Ensure that `RAW INPUT:` and its text are the very last content in the prompt (no trailing lines after it).
* Use only that raw text to build the output.
* Do not restate the input; return just the finished block.

RAW INPUT:
{clipboard}
